// Annotations (Type Annotations)
// Explicit type information you add to variables, functions, or parameters to tell TypeScript what type they should be.
// Example: let age: number = 20;

// Inference (Type Inference)
// When TypeScript automatically figures out the type without you writing it.
// Example: let age = 20; → TypeScript infers age is a number.